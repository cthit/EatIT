import React from "react";

import { DigitLayout, DigitLoading } from "@cthit/react-digit-components";

const Loading = () => (
    <DigitLayout.Center>
        <DigitLoading />
    </DigitLayout.Center>
);

export default Loading;
